# Web framework + ASGI server
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35

# Request parsing — Form()/multipart uploads need python-multipart
python-multipart>=0.0.18

# ORM + settings
sqlmodel>=0.0.22
pydantic-settings>=2.7,<3.0

# Database driver for the Neon Postgres target (local dev uses SQLite, no driver needed)
psycopg2-binary>=2.9.9

# Auth (JWT in httpOnly cookie, TDD §10). PyJWT is actively maintained and
# avoids python-jose's unmaintained `ecdsa` dependency (CVE-2024-23342).
PyJWT>=2.9,<3.0

# Room image storage (Render free tier has no persistent disk)
cloudinary>=1.41

# Tests (httpx powers FastAPI TestClient)
pytest>=8.3,<9.0
httpx>=0.27
